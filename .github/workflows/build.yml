
name: build
on: [push]
  # push:
  #   branches: [ main ]
  # pull_request:
  #   branches: [ main ]
 
jobs:
    
  install:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
      - run: npm ci
      - run: npm run build
      - run: npm run test

  # build:
  #   # runs-on: node:20
  #   needs: install
  #   steps:
  #     - run: npm run build

  # test:
  #   # runs-on: node:20
  #   needs:
  #     - install
  #     - build
  #   steps:
  #     - run: npm run test

  